/// Host navigation hooks for mini-apps and plugins.
class ShellNavigator {
  const ShellNavigator();

  Future<void> openMiniApp(String key) async {}

  Future<void> openDeepLink(String uri) async {}
}
