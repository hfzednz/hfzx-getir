# NEXORA scripts

| Script | Purpose |
|--------|---------|
| `bootstrap.ps1` / `bootstrap.sh` | Clone-and-run bootstrap |
| `verify-structure.ps1` | Assert required monorepo paths exist |
| `doctor.ps1` | Toolchain versions |
| `test-go-focus.ps1` | Critical Go service tests |

Prefer `make <target>` from repo root when Make is available.
