# Global Service Features

- Country hierarchy + places (region/state/city/district/postal/zone)
- Languages (RTL/LTR), locale format profiles, translation namespaces + versioning + AI assist port
- Currencies + historical FX + convert (display; settlement via finance/payment elsewhere)
- Holidays, regional business rules, tax *rules*, privacy regimes (GDPR/KVKK/CCPA-ready)
- Payment method *availability*, logistics *policy* hints, legal docs
- `GET /resolve` bootstrap bundle; TR seed; outbox `global.events`
