{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "global.ExchangeRateUpdated.v1",
  "type": "object",
  "required": ["type", "aggregateId", "tenantId", "occurredAt", "base", "quote", "rate"],
  "properties": {
    "type": { "const": "ExchangeRateUpdated" },
    "base": { "type": "string" },
    "quote": { "type": "string" },
    "rate": { "type": "number" },
    "aggregateId": { "type": "string", "format": "uuid" },
    "tenantId": { "type": "string", "format": "uuid" },
    "occurredAt": { "type": "string", "format": "date-time" }
  }
}
