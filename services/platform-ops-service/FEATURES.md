# Platform Ops Features

- Deployment lifecycle (start/complete/rollback) via GitOps port
- Scaling events via cluster port (HPA/KEDA orchestration intent)
- Backup jobs (Velero/pg dump port)
- Recovery / DR / chaos drill tracking
- Alert ingest, cost snapshots (minor units), SLO burn reporting
- Outbox events on `platform.events`
