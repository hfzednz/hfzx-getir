# NEXORA LiveOps Service

Feature flags, remote config, experiments, LiveOps calendar, rollbacks.

- HTTP `:8116` `/v1/liveops`
- Does **not** own campaigns, notifications, analytics warehouse, or AI model serving

```bash
make test && make run
```
