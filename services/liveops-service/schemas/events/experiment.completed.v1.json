{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "liveops.ExperimentCompleted.v1",
  "type": "object",
  "required": ["type", "aggregateId", "tenantId", "occurredAt", "key", "winner"],
  "properties": {
    "type": { "const": "ExperimentCompleted" },
    "key": { "type": "string" },
    "winner": { "type": "string" },
    "aggregateId": { "type": "string", "format": "uuid" },
    "tenantId": { "type": "string", "format": "uuid" },
    "occurredAt": { "type": "string", "format": "date-time" }
  }
}
