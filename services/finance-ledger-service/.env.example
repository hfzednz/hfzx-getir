# finance-ledger-service environment example (never commit real production secrets)

HTTP_ADDR=:8091
GRPC_ADDR=:9091
DATABASE_URL=
REDIS_URL=
KAFKA_BROKERS=
CORS_ALLOWED_ORIGINS=*
# Required for payment-service journal posts (header X-Ledger-Internal-Token).
LEDGER_INTERNAL_TOKEN=nexora-ledger-dev
