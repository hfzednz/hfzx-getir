# NEXORA Open Platform Service

Developer apps, API keys, webhook delivery, API catalog, gateway policy export, SDK registry.

- HTTP `:8120` `/v1/open`
- Does **not** own domain business logic or IAM credential SoT

```bash
make test && make run
```
