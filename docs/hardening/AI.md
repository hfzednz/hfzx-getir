# AI Optimization Guide

- Batch inference where latency budget allows
- Quantize/pruning for edge offline models (innovation edge stubs)
- Embedding + prompt response caches in Redis
- GPU autoscaling via platform-ops (ownership unchanged)
- Benchmark key: `ai_per_sec` ≥ 500
